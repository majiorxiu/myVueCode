const grid = {
  // 表格过滤内容，针对枚举字段,有问题，暂时无法使用
  //  filterKeyToValue (rows, enumList, _key) {
  //   let value = enumList.filter(item => item.TypeName === _key).TypeID
  //   if(value) {
  //     rows = rows.filter(item => item[key] === _key[key])
  //   }
  //   return rows
  // }
}

export default grid